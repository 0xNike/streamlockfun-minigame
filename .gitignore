node_modules/
dist/
*.log
.DS_Store

# Env files — NEVER commit
.env
.env.local
.env.*.local
.env.production
.env.development

# Operator signer keypairs — real-money secrets, NEVER commit
operator.json
mainnet-operator.json
*-operator.json

# Editor
.vscode/
.idea/
*.swp

# OS
Thumbs.db

# SQLite (operator session DB)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# TypeScript incremental build cache
*.tsbuildinfo

# MCP server config — contains API credentials
.mcp.json
.vercel

# Local, per-developer Claude Code settings (permissions allowlist)
.claude/settings.local.json
